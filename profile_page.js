$(function() {
    let splitUrl = window.location.pathname.split("/");
    id = splitUrl[splitUrl.length - 1];

    // $.get(apiUrl + id, function(data) {
    //     $("#name").text = data.name;
    //     ...
    // });
})